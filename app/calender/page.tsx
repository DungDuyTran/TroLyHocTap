// ScheduleForm → nhập liệu.

// RegisterSchedulePage → thêm/sửa/xóa (localStorage).

// SchedulePage → xem/sửa/xóa (localStorage).

// ScheduleView → component hiển thị dùng chung.

// ScheduleList → hiển thị lịch từ API/database qua ScheduleView.

// lịch học chỉ đang nằm trong localStorage của trình duyệt, tức là cục bộ, không chia sẻ giữa máy khác hoặc tài khoản khác.
