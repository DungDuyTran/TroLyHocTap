"use client";

import React from "react";
// Không cần import MainLayout, CozeChatWidget, LandingPage ở đây nữa
// vì chúng đã được xử lý bởi app/login/layout.tsx

export default function TrangWebPage() {
  return (
    <div>{/* Các thành phần chức năng chính của ứng dụng sẽ ở đây */}</div>
  );
}
